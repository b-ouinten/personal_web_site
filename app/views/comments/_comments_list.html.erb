<div>
  <% if @comments.count > 0 %>
    <h6 class="mb-4">Others leaft a comments</h6>
    <% @comments.each do |comment| %>
      <%= render 'comments/comment', comment: comment %>
    <% end %>
  <% end %>
</div>